# 🚀 Быстрый старт: Broadcast рассылка

## Шаг 1: Тестовый режим с примером

```bash
# Проверьте как работает система без реальной отправки
python -m bot.broadcast --file example_broadcast.txt --test
```

## Шаг 2: Отправьте себе для проверки

```bash
# Замените YOUR_TELEGRAM_ID на ваш telegram ID
python -m bot.broadcast --file example_broadcast.txt --users YOUR_TELEGRAM_ID
```

## Шаг 3: Создайте своё сообщение

```bash
# Создайте файл с вашим сообщением
nano my_message.txt
```

Пример содержимого:
```html
<b>🎄 Привет, :first_name!</b>

Мы рады сообщить о новых возможностях PhotoBot!

✨ <b>Что нового:</b>
• Ваше обновление здесь
• И ещё одно обновление

Спасибо, что с нами, :first_name! 🎄
```

## Шаг 4: Протестируйте своё сообщение

```bash
# Тестовый режим
python -m bot.broadcast --file my_message.txt --test

# Отправьте себе
python -m bot.broadcast --file my_message.txt --users YOUR_TELEGRAM_ID
```

## Шаг 5: Массовая рассылка

```bash
# Рассылка всем пользователям
python -m bot.broadcast --file my_message.txt
```

---

## 📝 Доступные переменные для персонализации

- `:first_name` - имя пользователя
- `:last_name` - фамилия пользователя
- `:username` - username пользователя

## 🎨 HTML форматирование

- `<b>жирный</b>` - **жирный текст**
- `<i>курсив</i>` - *курсив*
- `<code>код</code>` - `моноширинный`
- `<a href="URL">ссылка</a>` - [ссылка](URL)

---

📖 **Полная документация:** см. [BROADCAST.md](BROADCAST.md)
