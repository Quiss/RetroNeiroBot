# PhotoBot Broadcast - Инструкция по массовой рассылке

Система для отправки сообщений всем пользователям бота или конкретным пользователям.

## Возможности

- 📢 Рассылка всем пользователям из базы данных
- 🎯 Рассылка конкретным пользователям для тестирования
- 📄 Загрузка текста из файла
- 📝 Поддержка HTML форматирования (жирный, курсив, ссылки)
- 🎨 Персонализация сообщений с переменными
- 🧪 Тестовый режим для предпросмотра
- 📊 Подробная статистика отправки
- ⏱️ Задержка между сообщениями (защита от лимитов Telegram)

---

## Подготовка

### 1. Создайте файл с текстом сообщения

Создайте файл (например, `post1.txt`) с текстом, который хотите отправить:

```text
🎄 Привет, друзья PhotoBot! 🎄

Мы рады сообщить вам о новых возможностях нашего сервиса!

Спасибо, что выбрали PhotoBot! ✨
```

### 2. Форматирование текста (HTML)

Все сообщения поддерживают **HTML** форматирование для красивого оформления текста.

#### Доступные элементы форматирования:

| Формат | Синтаксис | Пример результата |
|--------|-----------|-------------------|
| **Жирный текст** | `<b>текст</b>` | **Важно!** |
| *Курсив* | `<i>текст</i>` | *Обратите внимание* |
| `Моноширинный` | `<code>текст</code>` | `код` |
| [Ссылка](url) | `<a href="URL">текст</a>` | [Перейти](https://example.com) |

#### Пример использования:

```html
<b>🎄 Важное обновление PhotoBot! 🎄</b>

Мы рады сообщить о <i>новых возможностях</i>:

• <b>Улучшена</b> качество генерации
• Добавлены новые стили обработки
• Оптимизирована <code>скорость работы</code>

🎁 Специальное предложение:
Получите <b>10 бонусных генераций</b> при покупке!

<a href="https://t.me/your_bot">Попробовать бота</a>
```

**Важные примечания:**
- HTML поддерживается во всех сообщениях автоматически
- Если синтаксис HTML некорректен, сообщение может не отправиться
- Используйте тестовый режим (`--test`) для проверки форматирования перед массовой рассылкой
- Специальные символы `<`, `>`, `&` имеют особое значение в HTML

### 3. Персонализация сообщений (опционально)

Вы можете персонализировать сообщения для каждого пользователя, используя специальные переменные:

#### Доступные переменные:

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `:first_name` | Имя пользователя | `"пользователь"` |
| `:last_name` | Фамилия пользователя | `""` (пустая строка) |
| `:username` | Username пользователя | `"пользователь"` |

#### Пример персонализированного сообщения:

**Файл:** `post_personalized.txt`

```html
Привет, :first_name! 👋

Рады видеть тебя в PhotoBot!

Специально для тебя, :username, мы подготовили новое обновление:

✨ Что нового:
• Улучшено качество генерации елочных игрушек
• Добавлены новые праздничные стили
• Оптимизирована скорость обработки

🎁 Персональное предложение:
Получи <b>5 бонусных генераций</b> бесплатно!

Спасибо, что выбрал PhotoBot, :first_name! 🎄
```

**Результат для пользователя "Андрей" (@andrew):**

```
Привет, Андрей! 👋

Рады видеть тебя в PhotoBot!

Специально для тебя, andrew, мы подготовили новое обновление:

✨ Что нового:
• Улучшено качество генерации елочных игрушек
• Добавлены новые праздничные стили
• Оптимизирована скорость обработки

🎁 Персональное предложение:
Получи 5 бонусных генераций бесплатно!

Спасибо, что выбрал PhotoBot, Андрей! 🎄
```

### 4. Комбинирование HTML и персонализации

Вы можете использовать HTML форматирование вместе с переменными персонализации:

```html
<b>Привет, :first_name!</b> 👋

Мы рады видеть тебя, <i>:username</i>, в PhotoBot!

✨ <b>Специально для тебя:</b>
• Получи <code>5 бесплатных генераций</code>
• <a href="https://t.me/your_bot">Попробовать сейчас</a>

<b>Спасибо, :first_name!</b> 🎄
```

---

## Использование

### Вариант 1: Локальный запуск

#### Рассылка всем пользователям

```bash
python -m bot.broadcast --file upgrade.txt
```

#### Рассылка конкретным пользователям (тестирование)

```bash
python -m bot.broadcast --file upgrade.txt --users 123456789,987654321
```

#### Тестовый режим (РЕКОМЕНДУЕТСЯ)

```bash
python -m bot.broadcast --file upgrade.txt --test
```

---

### Вариант 2: Запуск через Docker

#### Шаг 1: Рассылка всем пользователям

```bash
docker-compose exec bot python -m bot.broadcast --file /app/upgrade.txt
```

#### Шаг 2: Рассылка конкретным пользователям

```bash
docker-compose exec bot python -m bot.broadcast --file /app/upgrade.txt --users 123456789,987654321
```

---

## Параметры командной строки

| Параметр | Короткая форма | Описание | Обязательный |
|----------|----------------|----------|--------------|
| `--file` | `-f` | Путь к файлу с текстом сообщения | ✅ Да |
| `--users` | `-u` | Список telegram_id через запятую | ❌ Нет |
| `--test` | `-t` | Тестовый режим: показать кому и что будет отправлено без реальной отправки | ❌ Нет |

---

## Примеры

### Пример 1: Тестовый режим (РЕКОМЕНДУЕТСЯ ИСПОЛЬЗОВАТЬ ПЕРВЫМ)

```bash
# Проверьте сообщение и список получателей перед реальной отправкой
python -m bot.broadcast --file upgrade.txt --test
```

**Вывод в тестовом режиме:**
```
==================================================
🧪 ТЕСТОВЫЙ РЕЖИМ - реальная отправка НЕ будет выполнена
==================================================
📊 Будет отправлено 150 пользователям

📝 Текст сообщения (оригинал):
--------------------------------------------------
Привет, :first_name! 👋

Рады видеть тебя в PhotoBot!
...
--------------------------------------------------

🎨 Пример персонализированного сообщения для первого пользователя:
--------------------------------------------------
Привет, Андрей! 👋

Рады видеть тебя в PhotoBot!
...
--------------------------------------------------
Для: Андрей Иванов (@andrew)

👥 Список получателей (первые 10):
  1. telegram_id: 123456789 | @user1 | John
  2. telegram_id: 987654321 | @user2 | Jane
  ... и ещё 140 получателей

==================================================
✅ Тестовый просмотр завершён
💡 Для реальной отправки запустите без флага --test
==================================================
```

### Пример 2: Простая рассылка

```bash
# Создайте файл message.txt
echo "Привет! Это тестовое сообщение." > message.txt

# Запустите рассылку
python -m bot.broadcast --file message.txt
```

### Пример 3: Тестирование на конкретных пользователях

```bash
# Отправьте только конкретным пользователям для проверки
python -m bot.broadcast --file upgrade.txt --users 123456789,987654321
```

### Пример 4: Рассылка с персонализацией

```bash
# Используйте файл с переменными для персонализации
python -m bot.broadcast --file post_personalized.txt

# Сначала протестируйте персонализацию
python -m bot.broadcast --file post_personalized.txt --test

# Отправьте только себе для проверки
python -m bot.broadcast --file post_personalized.txt --users YOUR_TELEGRAM_ID
```

---

## Логирование

Логи рассылки сохраняются в:

- **Консоль**: Цветной вывод в реальном времени
- **Файл**: `logs/broadcast/YYYY-MM-DD_HH-MM-SS.txt` (отдельный файл для каждой рассылки)

Каждая рассылка создаёт свой лог-файл с датой и временем запуска, например:
- `logs/broadcast/2024-12-30_15-30-45.txt`
- `logs/broadcast/2024-12-30_16-15-22.txt`

### Пример вывода

```
15:30:45 | INFO     | 📝 Лог рассылки сохраняется в: logs/broadcast/2024-12-30_15-30-45.txt
15:30:45 | INFO     | 📄 Загружен текст сообщения из post1.txt
15:30:45 | INFO     | 📝 Длина сообщения: 245 символов
15:30:45 | INFO     | 📢 Получение всех пользователей для рассылки...
15:30:46 | INFO     | ✅ Всего загружено пользователей: 100
15:30:47 | INFO     | 📊 Начинаем рассылку для 100 пользователей
15:30:48 | INFO     | ✅ [1/100] Отправлено пользователю 123456789
15:30:49 | INFO     | ✅ [2/100] Отправлено пользователю 987654321
...
15:35:00 | INFO     | ==================================================
15:35:00 | INFO     | 📊 Рассылка завершена!
15:35:00 | INFO     | ✅ Успешно: 95
15:35:00 | INFO     | ❌ Ошибок: 5
15:35:00 | INFO     | 📈 Всего: 100
15:35:00 | INFO     | ==================================================
```

---

## Советы и рекомендации

### ✅ Рекомендуется

1. **ВСЕГДА используйте тестовый режим перед реальной рассылкой:**
   ```bash
   # Сначала проверьте что будет отправлено
   python -m bot.broadcast --file upgrade.txt --test

   # Только потом делайте реальную рассылку
   python -m bot.broadcast --file upgrade.txt
   ```

2. **Тестируйте на себе или небольшой группе:**
   ```bash
   python -m bot.broadcast --file upgrade.txt --users YOUR_ID
   ```

3. **Проверяйте текст перед массовой рассылкой:**
   - Нет опечаток
   - Корректное форматирование HTML
   - Ссылки работают
   - Переменные персонализации написаны правильно (`:first_name`, `:last_name`, `:username`)

4. **Используйте HTML для выделения важной информации:**
   - Заголовки и акценты: `<b>Важно!</b>`, `<b>Обновления:</b>`
   - Выделение действий: `<i>нажмите здесь</i>`, `<i>обратите внимание</i>`
   - Технические термины: `<code>промокод</code>`, `<code>PHOTO10</code>`
   - Ссылки на ресурсы: `<a href="https://t.me/yourbot">Перейти в бот</a>`

5. **Используйте персонализацию для важных сообщений:**
   - Приветственные сообщения: `<b>Привет, :first_name!</b>`
   - Специальные предложения: `Специально для тебя, <i>:username</i>`
   - Напоминания: `Эй, <b>:first_name</b>, не забудь создать елочную игрушку!`

### ❌ Не рекомендуется

1. **Не отправляйте слишком часто** - можно получить блокировку от Telegram
2. **Не отправляйте спам** - только полезный контент
3. **Не забывайте про лимиты Telegram** - скрипт имеет задержку 50ms между сообщениями

---

## Устранение проблем

### Проблема: "Файл не найден"

```
❌ Файл не найден: post1.txt
```

**Решение:** Убедитесь, что файл существует и путь указан правильно.

### Проблема: "Ошибка отправки пользователю"

```
❌ [5/100] Ошибка отправки пользователю 123456789: Forbidden: bot was blocked by the user
```

**Решение:** Пользователь заблокировал бота - это нормально. Скрипт продолжит работу.

### Проблема: Ошибки с HTML форматированием

```
❌ [5/100] Ошибка отправки пользователю 123456789: Bad Request: can't parse entities
```

**Решение:** Проверьте синтаксис HTML:
- Убедитесь, что все теги закрыты: `<b>текст</b>`
- Проверьте, что все `<a href="URL">ссылки</a>` имеют правильный формат
- Используйте тестовый режим для проверки: `--test`
- Если нужно использовать символы `<`, `>`, `&` как обычный текст, используйте HTML entities: `&lt;`, `&gt;`, `&amp;`

---

## Безопасность

⚠️ **ВАЖНО:**

- Храните `.env` в безопасности
- Не коммитьте файлы с сообщениями в Git (добавьте `*.txt` в `.gitignore`)
- Проверяйте логи на предмет утечки данных

---

## Поддержка

Если возникли вопросы или проблемы:

1. Проверьте логи в `logs/broadcast/` (найдите файл с датой и временем вашей рассылки)
2. Убедитесь, что база данных доступна
3. Проверьте настройки в `.env`

**Пример поиска логов:**
```bash
# Посмотреть все логи рассылок
ls -lt logs/broadcast/

# Посмотреть последний лог
tail -f logs/broadcast/$(ls -t logs/broadcast/ | head -n1)

# Найти логи за конкретную дату
ls logs/broadcast/2024-12-30*
```
